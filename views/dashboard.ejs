<!DOCTYPE html>
<html>
<title>Shelter Dashboard</title>

<body>
    <h1 style="text-align: center;">Welcome to Shelter Dashboard</h1>
    <hr style="width: 15cm;">

    <h2>Descision Logs: </h2> <p><a href="/dashboard/descisionlogs">Descision Logs</a></p>
    <h2>Shelter Home: </h2><p><a href="/home-shelter">Home</a></p>
    <h2 >Pending Applications:</h2>
    <form action="/dashboard" method="get">
        Filter by Pet: <select name="pet">
            <% let filterName = [] %>
            <option value="">--Select a Pet--</option>
            <% filterList.forEach(application => { %>               
                <% if(application.pet && !(filterName.includes(application.pet.name)) && application.applicant){ %>
                <option value="<%= application.pet.name %>"><%= application.pet.name %></option>
                <% filterName.push(application.pet.name) %>
                <% } %>
            <% }) %>            
        </select>
        <button type="submit">Filter/Reset Filter</button>
    </form>
    <br>
    <hr>
    

    <% if(dashboardList.length == 0){ %>
        <p>You got no pending applications!</p> 
        <% }else{ %>
        <% dashboardList.forEach(application=> { %>
        <% if (application.pet && application.applicant) { %>
            <p> Application ID: <%= application.id %> </p>
                <p> Applicant ID: <%= application.applicantName %> </p>
                    <p> Pet: <%= application.pet.name %> </p>
                        <p> Living Situation: <%= application.livingSituation %> </p>
                            <% if(application.experienceDetails.length==0){ %>
                                <p> Experience Details: Applicant has no experience details! </p>                             
                                <% }else{ %>
                                    <p> Experience Details: <%= application.experienceDetails %> </p>
                                        <% } %>
                                        <p>Application Status: <a href="/dashboard/applicationDetails/<%= application.id %>"><%= application.status %></a></p>
                                            <p> Application DateTime: <%= application.applicationDate %> </p>
                                                <hr>
                                                <% }; %>
                                            <% })}; %>
                                            
</body>

</html>